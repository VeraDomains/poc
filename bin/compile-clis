#!/bin/bash
. "$(dirname "${BASH_SOURCE[0]}")/_bootstrap.sh"

cd "${ROOT_DIR}/.."

function compile() {
    local module="$1"

    echo "Compiling ${module}..."
    ./gradlew ":${module}:installDist"
}

compile "vera-ca"
